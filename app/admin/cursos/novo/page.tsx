import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { AdminShell } from "@/components/admin/AdminShell"; import { CourseForm } from "@/components/admin/CourseForm";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/admin/login');const {data:staff}=await s.from('admin_profiles').select('role').eq('id',user.id).maybeSingle();if(!staff)redirect('/membro');return <AdminShell email={user.email}><div className="admin-heading"><div><span className="badge">MFB ACADEMY</span><h1>Novo curso</h1><p>Cadastre a formação. Módulos e aulas são adicionados na etapa seguinte.</p></div></div><CourseForm/></AdminShell>}
