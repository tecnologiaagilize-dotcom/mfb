import { AdminShell } from "@/components/admin/AdminShell"; import { createClient } from "@/lib/supabase/server"; import { redirect } from "next/navigation";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/admin/login');return <AdminShell email={user.email}><div className="admin-heading"><div><span className="badge">ORGANIZAÇÃO</span><h1>Categorias</h1><p>Organize os tipos de conteúdo e registros da plataforma sem alterar os dados já existentes.</p></div></div><section className="admin-panel"><div className="empty-cell">Estrutura reservada. As categorias serão normalizadas na próxima migração do banco.</div></section></AdminShell>}
