import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {AdminShell} from '@/components/admin/AdminShell';import {PollForm} from '@/components/admin/PollForm';export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/admin/login');const {data:a}=await s.from('admin_profiles').select('id').eq('id',user.id).maybeSingle();if(!a)redirect('/membro');return <AdminShell email={user.email}><div className="admin-heading"><div><span className="badge">NOVA CONSULTA</span><h1>Criar pesquisa</h1><p>As respostas serão voluntárias e apresentadas de forma agregada.</p></div></div><PollForm/></AdminShell>}
