import { AdminShell } from "@/components/admin/AdminShell"; import { createClient } from "@/lib/supabase/server"; import { redirect } from "next/navigation";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/admin/login');return <AdminShell email={user.email}><div className="admin-heading"><div><span className="badge">PLATAFORMA</span><h1>Configurações</h1><p>Central para parâmetros institucionais, integrações e preferências gerais do MFB.</p></div></div><section className="admin-panel"><div className="empty-cell">Configurações sensíveis serão habilitadas somente após a definição do ambiente Supabase de produção.</div></section></AdminShell>}
