import { AdminShell } from "@/components/admin/AdminShell"; import { createClient } from "@/lib/supabase/server"; import { redirect } from "next/navigation";
export default async function Page(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/admin/login');return <AdminShell email={user.email}><div className="admin-heading"><div><span className="badge">SEGURANÇA</span><h1>Auditoria</h1><p>O painel está reservado para o histórico de ações administrativas e alterações relevantes.</p></div></div><section className="admin-panel"><div className="empty-cell">A interface está pronta para receber o log de auditoria na migração de produção.</div></section></AdminShell>}
